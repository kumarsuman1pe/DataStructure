package DataStructure.Java8;

import java.util.*;
import java.util.stream.Collectors;

public class ArrayAndAngram {
//    public static Map<String, List<String>> findAnagramGroups(String[] arr) {
//        return Arrays.stream(arr)
//                .collect(Collectors.groupingBy().sorted().collect(Collectors.joining()));
//    }

//    public static void main(String[] args) {
//        String[] arr = {"java", "aavj", "abcd", "dbca", "avaj"};
//        Map<String, List<String>> anagramGroups = findAnagramGroups(arr);
//
//        for (Map.Entry<String, List<String>> entry : anagramGroups.entrySet()) {
//            System.out.println(entry.getKey() + " " + entry.getValue());
//        }
//    }

}
